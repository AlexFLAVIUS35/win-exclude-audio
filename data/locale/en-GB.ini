Name="Application Audio Exclusion"

ExecutableList="Applications to exclude"

ActiveSession.Group="Add from currently active applications"
ActiveSession.List="Application"
ActiveSession.Add="Add application"
ActiveSession.Refresh="Refresh list"

Status.Capturing="Capturing:"
Status.None="Not capturing"
Status.Excluding="Capturing everything except:"
Status.NoMatch="No running session matches:"
